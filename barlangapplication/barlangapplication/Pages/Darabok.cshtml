@page
@model barlangapplication.Pages.DarabokModel

@{
    ViewData["Title"] = "Darabok";
}

<h1>Darabok</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VarosokBarlangDTO[0].Varos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VarosokBarlangDTO[0].Darabszam)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.VarosokBarlangDTO) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Varos)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Darabszam)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
